services:
  excalidraw:
    image: excalidraw/excalidraw:latest
    restart: unless-stopped
    networks:
      - proxy
    labels:
      caddy: excalidraw.lab.wsiwiec.com #👈 Subdomain using wildcard cert
      caddy.import: auth
      caddy.reverse_proxy: "{{upstreams 80}}" #👈 Container port

networks:
  proxy:
    external: true
