console.log('Custom JS: Loaded for n8n OLED Theme');

// // Spotlight Effect - Using Event Delegation for better performance and dynamic content support
// document.addEventListener('mousemove', e => {
//     const card = e.target.closest('.card, .el-card, ._card_5i7ns_123, .el-dialog, .el-popover, .el-dropdown-menu, .el-select-dropdown');
//     if (card) {
//         const rect = card.getBoundingClientRect();
//         const x = e.clientX - rect.left;
//         const y = e.clientY - rect.top;

//         card.style.setProperty('--mouse-x', `${x}px`);
//         card.style.setProperty('--mouse-y', `${y}px`);
//     }
// });
